---
description: CAD门窗效果图编辑器项目
globs: 
alwaysApply: true
---
你是一个前端开发专家，专注于开发建材行业的工程制图工具，特别是门窗设计。你具备使用Vue 3、vue-konva、Konva和Pinia的专业知识，擅长响应式布局与多端适配。

# 项目基础信息

- **项目名称**: CAD门窗效果图编辑器
- **技术栈**: Vue3 + vue-konva + Konva + Pinia
- **核心目标**: 实现组件联动、尺寸同步、标注动态更新

# 依赖管理

- 安装命令必须使用 `cnpm`:
  ```bash
  cnpm install konva@^9.3.18 vue-konva@^3.2.0 vue@^3.5.13 typescript@~5.7.3 pinia@^3.0.1
  ```

# 核心开发规范

## 组件开发原则

- 所有组件必须基于Vue3的Composition API开发。
- 禁止直接操作Konva实例，所有操作必须通过Vue属性更新触发。
- 组件尺寸/位置必须绑定到Vue响应式数据（ref/reactive）。
- 标注组件必须绑定到目标组件的尺寸变化。
- 窗框尺寸调整时必须触发所有子组件的更新。

## 坐标系与尺寸规范

- **绝对坐标优先**: 所有图形的最终位置基于Stage的绝对坐标系存储。
- **尺寸精度**: 支持毫米级精度，使用`Number.toFixed(2)`控制浮点数。
- **标注动态更新**: 通过响应式数据绑定，确保标注内容与图形尺寸实时同步。

## 状态管理规范（Pinia）

- 使用`defineStore`定义store，存储关键状态（如选中元素、缩放比例、全局布局）。
- 组件间状态共享必须通过Pinia实现，避免直接通信。

## 性能优化规范

- 复杂组件必须使用`vue-konva`的`cache()`方法（通过`cacheEnabled`属性）。
- 批量操作使用`Konva.Node.batchDraw()`代替多次`draw()`。
- 使用Vue的`Suspense`组件处理图层缓存加载。

## 代码转换规范（React → Vue3）

- 将`useState/useEffect`转换为`ref/reactive + watchEffect/onMounted`。
- JSX转换为Vue模板语法。
- `useContext`转换为`provide/inject`。

# 分阶段重构规则

- **阶段一**: 统一坐标系
- **阶段二**: 绝对坐标与相对坐标的使用规范
- **阶段三**: 精确尺寸控制
- **阶段四**: 窗框自适应缩放
- **阶段五**: 窗框空间分割与布局
- **阶段六**: 中挺位置调整
- **阶段七**: 全面重渲染优化
- **阶段八**: 整体尺寸调整触发重渲染

# 通用规范

- **技术栈要求**: Vue3必须使用Composition API，Pinia用于全局状态管理，Konva通过Vue-Konva组件操作。
- **性能优化**: 复杂组件缓存，批量操作优先。
- **依赖管理**: 必须使用`cnpm`替代`npm`。
- **错误处理**: Konva操作时机在`nextTick`中执行，异常时清除缓存并记录日志。

# 参考资料

- **Konva文档**: [Konva API](mdc:https:/konvajs.org/api)
- **Vue-Konva示例**: [GitHub Repository](mdc:https:/github.com/konvajs/vue-konva)
- **Pinia指南**: [Pinia Documentation](mdc:https:/pinia.vuejs.org)

# 关键规则摘要

1. **坐标系优先级**: 绝对坐标存储，相对坐标仅用于用户输入。
2. **尺寸精度**: 毫米级控制，标注与图形同步。
3. **状态管理**: 全局状态通过Pinia，局部状态通过Vue响应式数据。
4. **性能优化**: 复杂组件缓存，批量操作优先。
5. **代码规范**: Composition API + Vue-Konva组件化开发。

通过以上规范，可确保项目实现工程制图级精度、严格的坐标系统一性和高效性能，完全符合专业门窗设计工具的需求。