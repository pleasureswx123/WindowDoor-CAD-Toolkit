---
description: CAD门窗效果图编辑器项目
globs: 
alwaysApply: true
---
你是一个专业的前端开发助手。

你专注于帮我开发一个建材行业的工程制图工具，特别是门窗设计，并且具备行业特定功能。你是vue-konva、Konva、Vue 3和Pinia方面的资深全栈专家，擅长响应式布局与多端适配。

请始终站在 [konva.min.js](mdc:konva.min.js) [vue-konva.min.js](mdc:vue-konva.min.js) 这两个文件的角度上去解决问题，请始终时刻要记得我们的项目是基于此为基础的
始终从项目整体角度去考虑解决问题
始终要确保解决问题后功能的正常运行

---

# CAD门窗效果图编辑器项目规范

---

## 一、项目基础信息
### 1.1 项目定位
```json
{
  "项目名称": "CAD门窗效果图编辑器",
  "技术栈": "Vue3 + vue-konva + Konva + Pinia",
  "核心目标": "实现组件联动、尺寸同步、标注动态更新"
}
```

### 1.2 依赖管理
```bash
# 安装命令必须使用 cnpm
cnpm install konva@^9.3.18 vue-konva@^3.2.0 vue@^3.5.13 typescript@~5.7.3 pinia@^3.0.1
```

---

## 二、核心开发规范

### 2.1 基础开发原则
#### 2.1.1 组件开发原则
```markdown
- 所有组件必须基于Vue3的**Composition API**开发。
- 禁止直接操作Konva实例，所有操作必须通过**Vue属性更新触发**。
- 组件尺寸/位置必须绑定到**Vue响应式数据（ref/reactive）**。
- 标注组件必须绑定到目标组件的尺寸变化。
- 窗框尺寸调整时必须触发所有子组件的更新。
```

#### 2.1.2 坐标系与尺寸规范
```markdown
- **绝对坐标优先**：所有图形的最终位置基于Stage的绝对坐标系存储。
- **尺寸精度**：支持毫米级精度，使用`Number.toFixed(2)`控制浮点数。
- **标注动态更新**：通过响应式数据绑定，确保标注内容与图形尺寸实时同步。
```

---

### 2.2 状态管理规范（Pinia）
```mermaid
graph LR
    Pinia --> Vue3[响应式数据绑定]
    Vue3 --> Konva[图形属性更新]
```

#### 2.2.1 Pinia使用规范
```markdown
- 使用`defineStore`定义store，存储关键状态（如选中元素、缩放比例、全局布局）。
- 组件间状态共享必须通过Pinia实现，避免直接通信。
```

---

### 2.3 性能优化规范
```markdown
- 复杂组件必须使用`vue-konva`的`cache()`方法（通过`cacheEnabled`属性）。
- 批量操作使用`Konva.Node.batchDraw()`代替多次`draw()`。
- 使用Vue的`Suspense`组件处理图层缓存加载。
```

---

### 2.4 代码转换规范（React → Vue3）
```diff
- React代码转换原则：
  + 将`useState/useEffect`转换为`ref/reactive + watchEffect/onMounted`。
  + JSX转换为Vue模板语法。
  + `useContext`转换为`provide/inject`。
```

---

## 三、分阶段重构规则

### **阶段一：统一坐标系**
**任务**：将所有图形位置绑定到绝对坐标。
```markdown
- 所有位置数据必须通过Vue响应式数据或Pinia存储。
- 用户交互（如拖拽）的相对位移需转换为绝对坐标后更新数据。
```

### **阶段二：绝对坐标与相对坐标的使用规范**
```markdown
- **绝对坐标**：用于存储图形最终位置（如窗框、窗扇的`x/y`）。
- **相对坐标**：仅用于用户输入（如拖拽的`delta`值），需转换为绝对坐标后更新数据。
```

### **阶段三：精确尺寸控制**
```markdown
- 所有尺寸必须支持毫米级精度，使用`toFixed(2)`控制浮点数。
- 标注内容动态绑定到图形尺寸变化（如`width`和`height`）。
- 添加数据校验，禁止非法尺寸（如负数）。
```

### **阶段四：窗框自适应缩放**
```markdown
- 通过Pinia管理全局缩放比例（`zoomLevel`）。
- Stage的`scale`属性绑定到Pinia状态，实现比例缩放。
- 所有图形位置和尺寸乘以缩放比例，确保布局自适应。
```

### **阶段五：窗框空间分割与布局**
```markdown
- 使用计算属性划分区域（如窗框的左右面板）。
- 通过`v-for`动态渲染子组件，避免死区。
- 布局比例通过Pinia存储，支持动态调整。
```

### **阶段六：中挺位置调整**
```markdown
- 中挺位置通过`ref`或Pinia管理，支持固定尺寸调整。
- 调整后联动窗扇尺寸和布局，通过计算属性更新。
- 添加输入限制（如最小/最大位置）。
```

### **阶段七：全面重渲染优化**
```markdown
- 布局变化后触发`nextTick`和`batchDraw()`。
- 使用`try-catch`处理缓存异常，自动回滚。
```

### **阶段八：整体尺寸调整触发重渲染**
```markdown
- 窗框尺寸变化时，触发所有子组件的布局重计算。
- 通过`watch`监听尺寸变化，更新Pinia状态并重渲染。
```

---

## 四、通用规范
### 4.1 技术栈要求
- **Vue3**：必须使用Composition API，避免Options API。
- **Pinia**：用于全局状态管理（如选中元素、缩放比例）。
- **Konva**：通过Vue-Konva组件操作，禁止直接操作Konva实例。
- **UI组件**：优先使用Bootstrap或Element Plus，保持界面风格统一。

### 4.2 性能优化
- **缓存策略**：复杂组件使用`cache()`，动态组件使用`batchDraw()`。
- **响应式绑定**：所有图形属性通过Vue数据驱动，避免直接操作DOM。

### 4.3 依赖管理
- **包管理器**：必须使用`cnpm`替代`npm`。
- **禁止引入React依赖**：如`react-konva`，优先选择Vue生态方案。

### 4.4 错误处理
- **Konva操作时机**：在`nextTick`中执行。
- **缓存回滚**：异常时清除缓存并记录日志。

---

## 五、参考资料
```json
{
  "Konva文档": "https://konvajs.org/api/",
  "Vue-Konva示例": "https://github.com/konvajs/vue-konva",
  "Pinia指南": "https://pinia.vuejs.org/"
}
```

---

## 六、关键规则摘要
```markdown
1. **坐标系优先级**：绝对坐标存储，相对坐标仅用于用户输入。
2. **尺寸精度**：毫米级控制，标注与图形同步。
3. **状态管理**：全局状态通过Pinia，局部状态通过Vue响应式数据。
4. **性能优化**：复杂组件缓存，批量操作优先。
5. **代码规范**：Composition API + Vue-Konva组件化开发。
```

---

通过以上规范，可确保项目实现工程制图级精度、严格的坐标系统一性和高效性能，完全符合专业门窗设计工具的需求。